# Catalog P3 Implementation Plan — 周期对账 + 停用闭环 + export + 明文扫描

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 补齐声明式目录的后台 controller loop 与 `pending_deactivate` 消费闭环——周期对账 job + `export-catalog` CLI + 明文密钥扫描 + 控制台确认停用。

**Architecture:** 新增 DB 生命周期终态 `deactivated`(墓碑,从可编辑 YAML 过滤);`catalog-reconcile-service` 加只读 `detectDrift`(pending_deactivate backlog)+ 一个周期 job 挂 BullMQ;`catalog-service` 加 `confirmDeactivate`(软停用,不动 KC、不撤销 assignment);`export-catalog` 镜像 `apply-catalog`;`secret-scan` 在 apply 校验硬拦明文。前端在 `/admin/catalog` 加「待停用」面板。

**Tech Stack:** Next.js 16、Drizzle ORM(status = `text()` + 注释,非 pgEnum)、zod v4、TanStack react-query v5、next-intl、shadcn/sonner、BullMQ+Redis worker、Vitest(unit + integration 真 PG/KC)、Playwright e2e。

设计之据:`docs/implementation/plans/2026-07-07-catalog-p3-design.md`。分支 `feat/catalog-p3`(已从 origin/main `7f266d4` 切,spec 已提交 `4795fb0`),PR base `main`,**末尾一个合并 PR**。工作目录 `identity-center/identity-portal`(pnpm)。

## Global Constraints

以下为项目级硬约束,**每个任务都隐含包含**(逐字复制自 spec §8):

- `SecretFinding`、日志、错误**绝不含命中的密文子串**——只出路径 + 模式名 + 提示。
- export / YAML / 扫描输出**只出 `secretRef`(env 变量名),绝不明文**。
- `confirmDeactivate` **不调用 KC、不撤销 assignment**(软停用,承接 D2 / spec §15#3)。
- 周期 job **不自动确认停用**(纯人工)。
- **不改 CSP**(不引入 `unsafe-eval` 等)。
- `status` 恒为 `text()` + 注释,**永不用 pgEnum**(KES 兼容);新增 `deactivated` 是 text 值,**无需 migration**。
- `deactivated` 是 **DB-only** 生命周期态,**永不进 YAML schema**(`catalogAppSchema.status` 保持 `z.enum(['active','disabled'])`,不动)。
- Next.js 16 有破坏性变更:改动 page/route 前读 `node_modules/next/dist/docs/`;路由 params 是 `Promise`。
- i18n 文案 en + zh-CN **双语对齐**(parity 测试强制)。
- 已有 `catalog:read` / `catalog:apply` 权限(P2 seed),复用,不新建权限。
- eslint **禁止 setState-in-useEffect**;前端持有服务端数据用渲染期 `if (data !== loaded)` 守卫(见 `catalog-view.tsx`)。

**测试命令:**
- 单元:`pnpm test tests/unit/<file>`(vitest 按路径过滤)。
- 集成:`pnpm test:integration`(跑**全量** suite;新测必须过;已知 flake 容忍:api-contract 429 / remediate-email-state / mq-adapter / keycloak-admin,均在隔离运行时过)。
- e2e:`pnpm exec playwright test tests/e2e/<file>`。
- 静态:`pnpm typecheck` · `pnpm lint` · `pnpm build`。

---

## File Structure

**新建:**
- `lib/catalog/secret-scan.ts` — 明文密钥扫描(纯函数,遍历 CatalogDoc 字符串叶子)。
- `server/jobs/reconcile-catalog.ts` — 周期对账 job(纯函数 `(ctx: JobContext) => JobResult`)。
- `scripts/export-catalog.ts` — DB → YAML CLI。
- `app/api/admin/catalog/pending-deactivate/route.ts` — GET 待停用清单(catalog:read)。
- `app/api/admin/catalog/deactivate/route.ts` — POST 确认停用(catalog:apply)。
- `features/catalog/pending-deactivate-panel.tsx` — 控制台「待停用」面板。
- 测试:`tests/unit/catalog-serialize-deactivated.test.ts`、`tests/unit/catalog-secret-scan.test.ts`、`tests/integration/catalog-p3.test.ts`、`tests/e2e/catalog-pending-deactivate.spec.ts`。

**修改:**
- `lib/catalog/serialize.ts` — `toCatalogApps` 过滤 `deactivated`(app + role)。
- `db/schema/applications.ts` — `applications.status` / `applicationRoles.status` 注释加 `deactivated`。
- `server/services/catalog-reconcile-service.ts` — 加 `detectDrift`、`countActiveAssignments`、`loadActiveReconcileApps`、相关类型。
- `server/services/catalog-service.ts` — apply 内接入 secret-scan;返回对象加 `confirmDeactivate`。
- `scripts/worker.ts` — SCHEDULES + handlers 注册 `reconcile-catalog`。
- `features/catalog/queries.ts` — 加 `usePendingDeactivate` / `useConfirmDeactivate` / 类型 / key。
- `features/catalog/catalog-view.tsx` — 挂载 `<PendingDeactivatePanel/>`。
- `messages/en/catalog.json` + `messages/zh-CN/catalog.json` — 加 `pendingDeactivate` 文案。
- `package.json` — 加 `export-catalog` 脚本。

---

## Task 1: status 四态 + serialize 过滤 deactivated

**Files:**
- Modify: `lib/catalog/serialize.ts:73-91`(`toCatalogApps`)
- Modify: `db/schema/applications.ts:13`(applications.status 注释)、`:36`(applicationRoles.status 注释)
- Test: `tests/unit/catalog-serialize-deactivated.test.ts`(新建)

**Interfaces:**
- Produces: `toCatalogApps(appRows, roleRows)` 现在把 `status === 'deactivated'` 的 app 与 role 一并排除(此前只排除 `pending_deactivate`)。下游 `getCurrent`/`export` 因此不含 `deactivated`。

- [ ] **Step 1: 写失败单测** — `tests/unit/catalog-serialize-deactivated.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { toCatalogApps } from '@/lib/catalog/serialize'

const app = (code: string, status: string) => ({
  id: code, code, name: code.toUpperCase(), status,
  keycloakClientId: `c-${code}`, accessClientRole: `r-${code}`,
  webhookUrl: null, webhookSecretRef: null, loginUrl: null, adminUrl: null,
})

describe('toCatalogApps 过滤 deactivated', () => {
  it('排除 deactivated 应用,保留 active/disabled', () => {
    const out = toCatalogApps([app('a', 'active'), app('b', 'deactivated'), app('c', 'disabled')], [])
    expect(out.map((a) => a.code)).toEqual(['a', 'c'])
  })
  it('排除 deactivated 角色', () => {
    const roles = [
      { id: 'r1', applicationId: 'a', code: 'keep', name: 'Keep', description: null, status: 'active' },
      { id: 'r2', applicationId: 'a', code: 'gone', name: 'Gone', description: null, status: 'deactivated' },
    ]
    const out = toCatalogApps([app('a', 'active')], roles)
    expect(out[0].roles.map((r) => r.code)).toEqual(['keep'])
  })
})
```

- [ ] **Step 2: 跑测确认失败** — `pnpm test tests/unit/catalog-serialize-deactivated.test.ts` → FAIL(deactivated 仍被包含)。

- [ ] **Step 3: 改 serialize** — `lib/catalog/serialize.ts`,`toCatalogApps` 内两处 filter:
  - 第 75 行 app filter:`.filter((a) => a.status !== 'pending_deactivate' && a.status !== 'deactivated')`
  - 第 88 行 role filter:`.filter((r) => r.applicationId === a.id && r.status !== 'pending_deactivate' && r.status !== 'deactivated')`

- [ ] **Step 4: 改 schema 注释** — `db/schema/applications.ts`,两处 status 注释改为:
  `/** active | disabled | pending_deactivate | deactivated(pending_deactivate 由 catalog reconcile 置待人工确认;deactivated 为确认停用后的终态墓碑) */`

- [ ] **Step 5: 跑测确认通过** — `pnpm test tests/unit/catalog-serialize-deactivated.test.ts` → PASS。`pnpm typecheck` → 无错。

- [ ] **Step 6: Commit**

```bash
git add lib/catalog/serialize.ts db/schema/applications.ts tests/unit/catalog-serialize-deactivated.test.ts
git commit -m "feat(catalog): status 四态 + toCatalogApps 过滤 deactivated"
```

---

## Task 2: 明文密钥扫描 + 接入 apply

**Files:**
- Create: `lib/catalog/secret-scan.ts`
- Modify: `server/services/catalog-service.ts`(apply 内、parse 后 materialize 前接入)
- Test: `tests/unit/catalog-secret-scan.test.ts`(新建);集成断言加在 Task 5 的 `tests/integration/catalog-p3.test.ts`,此处仅补一条到现有 `tests/integration/catalog-api.test.ts` 亦可——本任务只做单元 + apply 接入,集成留待有 harness 的 Task 5。

**Interfaces:**
- Produces: `scanForPlaintextSecrets(doc: CatalogDoc): SecretFinding[]`;`SecretFinding = { path: string; pattern: string; hint: string }`(**绝不含命中值**)。apply 命中即抛 `ApiError('VALIDATION_ERROR', ...)`。

- [ ] **Step 1: 写失败单测** — `tests/unit/catalog-secret-scan.test.ts`:

```ts
import { describe, it, expect } from 'vitest'
import { scanForPlaintextSecrets, type SecretFinding } from '@/lib/catalog/secret-scan'
import type { CatalogDoc } from '@/lib/catalog/schema'

const doc = (url: string): CatalogDoc => ({
  version: 1,
  applications: [{
    code: 'x', name: 'X', status: 'active',
    keycloak: { clientId: 'cx', accessRole: 'rx' },
    webhook: { url, secretRef: 'X_SECRET' },
    roles: [],
  }],
})

describe('scanForPlaintextSecrets', () => {
  it('干净配置无 finding', () => {
    expect(scanForPlaintextSecrets(doc('https://x.example/hook'))).toEqual([])
  })
  it('命中 URL 里的 token 参数', () => {
    const f = scanForPlaintextSecrets(doc('https://x.example/hook?token=abcd1234secret'))
    expect(f.length).toBe(1)
    expect(f[0].path).toBe('applications[0].webhook.url')
    expect(f[0].pattern).toBe('url-credential')
  })
  it('命中 Bearer / PEM / AKIA / JWT', () => {
    expect(scanForPlaintextSecrets(doc('Bearer abcdefgh12345')).length).toBe(1)
    expect(scanForPlaintextSecrets(doc('-----BEGIN PRIVATE KEY-----')).length).toBe(1)
    expect(scanForPlaintextSecrets(doc('AKIAIOSFODNN7EXAMPLE')).length).toBe(1)
    expect(scanForPlaintextSecrets(doc('eyJhbGciOi.eyJzdWIiOi.sig12345')).length).toBe(1)
  })
  it('finding 绝不含命中的密文子串(安全铁律)', () => {
    const secret = 'abcd1234supersecretvalue'
    const f: SecretFinding[] = scanForPlaintextSecrets(doc(`https://x/h?token=${secret}`))
    const blob = JSON.stringify(f)
    expect(blob).not.toContain(secret)
    expect(blob).not.toContain('supersecret')
  })
})
```

- [ ] **Step 2: 跑测确认失败** — `pnpm test tests/unit/catalog-secret-scan.test.ts` → FAIL(模块不存在)。

- [ ] **Step 3: 实现 secret-scan** — `lib/catalog/secret-scan.ts`:

```ts
// lib/catalog/secret-scan.ts —— 对入库配置的明文密钥纵深防御。
// 铁律:finding 只出路径 + 模式名 + 提示,绝不回显命中的密文子串。
import type { CatalogDoc } from './schema'

export type SecretFinding = { path: string; pattern: string; hint: string }

const PATTERNS: Array<{ name: string; hint: string; re: RegExp }> = [
  { name: 'bearer-token', hint: 'Authorization Bearer', re: /\bBearer\s+[A-Za-z0-9._~+/-]{8,}=*/ },
  { name: 'url-credential', hint: 'token/key/secret in URL', re: /[?&](token|key|secret|password|api[_-]?key|access[_-]?token)=[^&\s]+/i },
  { name: 'pem-private-key', hint: 'PEM private key block', re: /-----BEGIN (?:RSA |EC |OPENSSH |DSA |PGP )?PRIVATE KEY-----/ },
  { name: 'aws-access-key', hint: 'AWS access key id', re: /\bAKIA[0-9A-Z]{16}\b/ },
  { name: 'jwt', hint: 'JWT', re: /\beyJ[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{6,}/ },
]

function walk(value: unknown, path: string, visit: (path: string, s: string) => void): void {
  if (typeof value === 'string') visit(path, value)
  else if (Array.isArray(value)) value.forEach((v, i) => walk(v, `${path}[${i}]`, visit))
  else if (value && typeof value === 'object')
    for (const [k, v] of Object.entries(value)) walk(v, path ? `${path}.${k}` : k, visit)
}

export function scanForPlaintextSecrets(doc: CatalogDoc): SecretFinding[] {
  const findings: SecretFinding[] = []
  walk(doc, '', (path, s) => {
    for (const p of PATTERNS) {
      if (p.re.test(s)) findings.push({ path, pattern: p.name, hint: p.hint }) // 注意:不 push s
    }
  })
  return findings
}
```

- [ ] **Step 4: 跑测确认通过** — `pnpm test tests/unit/catalog-secret-scan.test.ts` → PASS。

- [ ] **Step 5: 接入 apply** — `server/services/catalog-service.ts`:
  - 顶部 import 追加:`import { scanForPlaintextSecrets } from '@/lib/catalog/secret-scan'`。
  - `apply(input)` 内,`const doc = parseCatalogYamlOrThrow(input.yaml)` 之后**紧接**加:

```ts
      const secretFindings = scanForPlaintextSecrets(doc)
      if (secretFindings.length > 0) {
        throw new ApiError('VALIDATION_ERROR', 'catalog YAML 疑似含明文密钥(应改用 secretRef)', {
          issues: secretFindings.map((f) => ({ path: f.path, message: `疑似明文密钥(${f.hint})` })),
        })
      }
```

- [ ] **Step 6: 验证不回归** — `pnpm typecheck` → 无错;`pnpm test tests/unit/catalog-secret-scan.test.ts` → PASS。(apply 的集成断言在 Task 5 补,那里已有真实 PG/KC harness。)

- [ ] **Step 7: Commit**

```bash
git add lib/catalog/secret-scan.ts server/services/catalog-service.ts tests/unit/catalog-secret-scan.test.ts
git commit -m "feat(catalog): 明文密钥扫描 + apply 校验硬拦"
```

---

## Task 3: detectDrift + 周期对账 job + worker 注册

**Files:**
- Modify: `server/services/catalog-reconcile-service.ts`(加 `PendingDeactivateItem`/`DriftReport` 类型、`countActiveAssignments`、`loadActiveReconcileApps`、`detectDrift`)
- Create: `server/jobs/reconcile-catalog.ts`
- Modify: `scripts/worker.ts`(import + SCHEDULES + handlers)
- Test: `tests/integration/catalog-p3.test.ts`(新建;detectDrift 段)

**Interfaces:**
- Consumes: `ServiceContext = { db, keycloak }`(`./context`);`ReconcileApp = { code, keycloakClientId, accessClientRole }`;`ensureKeycloakRoles(apps)`(已存在);`JobContext = { db, mq, keycloak }`(`@/server/jobs/types`);`JobResult = { processed, failed, details? }`。
- Produces:
  - `export type PendingDeactivateItem = { kind: 'app' | 'role'; appCode: string; roleCode?: string; name: string; affectedAssignments: number }`
  - `export type DriftReport = { pendingDeactivate: PendingDeactivateItem[] }`
  - `export async function countActiveAssignments(db: ServiceDb, target: { appId: string } | { roleId: string }): Promise<number>`
  - `export async function loadActiveReconcileApps(db: ServiceDb): Promise<ReconcileApp[]>`
  - `createCatalogReconcileService(ctx).detectDrift(): Promise<DriftReport>`
  - `export async function reconcileCatalog(ctx: JobContext): Promise<JobResult>`

- [ ] **Step 1: 写失败集成测** — `tests/integration/catalog-p3.test.ts`(复制 `tests/integration/catalog-api.test.ts:1-70` 的 harness 头:`vi.mock('@/lib/auth', …)` 含 `ADMIN_CONSOLE_ROLE`、`createMigratedTestDb`、`resolveAdminApiConfig`、`__setServiceContextForTests`、`seedAdminRbac` + platform_admin 绑定;`beforeAll`/`afterAll` 同款)。detectDrift 段:

```ts
import { createCatalogReconcileService } from '@/server/services/catalog-reconcile-service'
import { createCatalogService } from '@/server/services/catalog-service'

// …harness(mockSession/tdb/ctx/beforeAll/afterAll)同 catalog-api.test.ts…

describe('detectDrift(pending_deactivate backlog)', () => {
  it('apply 移除应用后,detectDrift 列出该 app', async () => {
    // 先建一个 app
    await createCatalogService(ctx).apply({
      yaml: `version: 1\napplications:\n  - code: p3app\n    name: P3\n    keycloak: { clientId: p3app-client, accessRole: p3_access }\n    roles: []\n`,
      source: 'cli',
    })
    // 再 apply 空目录 → p3app 变 pending_deactivate
    await createCatalogService(ctx).apply({ yaml: `version: 1\napplications: []\n`, source: 'cli' })
    const drift = await createCatalogReconcileService(ctx).detectDrift()
    const codes = drift.pendingDeactivate.filter((x) => x.kind === 'app').map((x) => x.appCode)
    expect(codes).toContain('p3app')
    const item = drift.pendingDeactivate.find((x) => x.appCode === 'p3app')
    expect(item?.affectedAssignments).toBe(0)
  })
})
```

- [ ] **Step 2: 跑测确认失败** — `pnpm test:integration` → FAIL(`detectDrift` 不存在)。

- [ ] **Step 3: 实现 reconcile-service 增补** — `server/services/catalog-reconcile-service.ts`,顶部 import 与新导出:

```ts
import { and, count, eq } from 'drizzle-orm'
import * as schema from '@/db/schema'
import type { ServiceContext, ServiceDb } from './context'

// …保留现有 ReconcileApp / ReconcileReport…

export type PendingDeactivateItem = {
  kind: 'app' | 'role'
  appCode: string
  roleCode?: string
  name: string
  affectedAssignments: number
}
export type DriftReport = { pendingDeactivate: PendingDeactivateItem[] }

/** 统计目标当前 active 的受影响 assignment 数(app 级 applicationAssignments / role 级 applicationUserRoles) */
export async function countActiveAssignments(
  db: ServiceDb,
  target: { appId: string } | { roleId: string },
): Promise<number> {
  if ('appId' in target) {
    const [r] = await db
      .select({ value: count() })
      .from(schema.applicationAssignments)
      .where(and(eq(schema.applicationAssignments.applicationId, target.appId), eq(schema.applicationAssignments.status, 'active')))
    return r?.value ?? 0
  }
  const [r] = await db
    .select({ value: count() })
    .from(schema.applicationUserRoles)
    .where(and(eq(schema.applicationUserRoles.applicationRoleId, target.roleId), eq(schema.applicationUserRoles.status, 'active')))
  return r?.value ?? 0
}

/** active 应用 → ReconcileApp[](周期 job 的 ensure 输入源) */
export async function loadActiveReconcileApps(db: ServiceDb): Promise<ReconcileApp[]> {
  const apps = await db.query.applications.findMany({ where: eq(schema.applications.status, 'active') })
  return apps.map((a) => ({ code: a.code, keycloakClientId: a.keycloakClientId, accessClientRole: a.accessClientRole }))
}
```

  在 `createCatalogReconcileService(ctx)` 返回对象里,`ensureKeycloakRoles` 旁加 `detectDrift`:

```ts
    async detectDrift(): Promise<DriftReport> {
      const [pendingApps, pendingRoles, allApps] = await Promise.all([
        ctx.db.query.applications.findMany({ where: eq(schema.applications.status, 'pending_deactivate') }),
        ctx.db.query.applicationRoles.findMany({ where: eq(schema.applicationRoles.status, 'pending_deactivate') }),
        ctx.db.query.applications.findMany(),
      ])
      const codeById = new Map(allApps.map((a) => [a.id, a.code]))
      const items: PendingDeactivateItem[] = []
      for (const a of pendingApps) {
        items.push({ kind: 'app', appCode: a.code, name: a.name, affectedAssignments: await countActiveAssignments(ctx.db, { appId: a.id }) })
      }
      for (const r of pendingRoles) {
        items.push({ kind: 'role', appCode: codeById.get(r.applicationId) ?? '', roleCode: r.code, name: r.name, affectedAssignments: await countActiveAssignments(ctx.db, { roleId: r.id }) })
      }
      return { pendingDeactivate: items }
    },
```

- [ ] **Step 4: 跑测确认通过** — `pnpm test:integration` → detectDrift 段 PASS(其余仅容忍已知 flake)。

- [ ] **Step 5: 实现 job** — `server/jobs/reconcile-catalog.ts`:

```ts
// server/jobs/reconcile-catalog.ts —— 周期把 KC 实际态对齐到 active 应用,并汇报 pending_deactivate backlog。
// 与 reconcile.ts 同构;不自动确认停用(纯人工)。
import type { JobContext, JobResult } from './types'
import { createCatalogReconcileService, loadActiveReconcileApps } from '@/server/services/catalog-reconcile-service'

export async function reconcileCatalog(ctx: JobContext): Promise<JobResult> {
  const svc = createCatalogReconcileService({ db: ctx.db, keycloak: ctx.keycloak })
  const apps = await loadActiveReconcileApps(ctx.db)
  const ensured = await svc.ensureKeycloakRoles(apps)
  const drift = await svc.detectDrift()
  if (ensured.clientMissing.length) console.error(`[reconcile-catalog] KC 缺 client: ${ensured.clientMissing.join(', ')}`)
  for (const e of ensured.errors) console.error(`[reconcile-catalog] ensure 失败 ${e.appCode}: ${e.message}`)
  if (drift.pendingDeactivate.length) console.warn(`[reconcile-catalog] 待确认停用 ${drift.pendingDeactivate.length} 项`)
  return {
    processed: ensured.ensured.length,
    failed: ensured.clientMissing.length + ensured.errors.length,
    details: { ensured: ensured.ensured, clientMissing: ensured.clientMissing, errors: ensured.errors, pendingDeactivate: drift.pendingDeactivate },
  }
}
```

- [ ] **Step 6: worker 注册** — `scripts/worker.ts`:
  - import 追加:`import { reconcileCatalog } from '@/server/jobs/reconcile-catalog'`
  - `SCHEDULES` 数组末尾加:`{ name: 'reconcile-catalog', everyMs: Number(process.env.JOB_RECONCILE_CATALOG_MS ?? 3_600_000) },`
  - `handlers` map 加:`'reconcile-catalog': reconcileCatalog,`

- [ ] **Step 7: 验证** — `pnpm typecheck` → 无错;`pnpm lint` → 无错。(job 无独立 CLI,运行验证靠 Step 4 的 detectDrift 集成测 + 类型检查确认 worker 注册无误。)

- [ ] **Step 8: Commit**

```bash
git add server/services/catalog-reconcile-service.ts server/jobs/reconcile-catalog.ts scripts/worker.ts tests/integration/catalog-p3.test.ts
git commit -m "feat(catalog): detectDrift + reconcile-catalog 周期 job + worker 注册"
```

---

## Task 4: export-catalog CLI

**Files:**
- Create: `scripts/export-catalog.ts`
- Modify: `package.json`(scripts 加 `export-catalog`)
- Test: `tests/integration/catalog-p3.test.ts`(加 export round-trip 段)

**Interfaces:**
- Consumes: `createCatalogService(ctx).getCurrent()`;`createDbClient`(`@/lib/db/client`);`createKeycloakAdmin`/`keycloakConfigFromEnv`(`@/lib/keycloak/admin-client`);`scanForPlaintextSecrets`(Task 2);`parseCatalogYaml`(`@/lib/catalog/serialize`)。
- Produces: `pnpm export-catalog [-- --out <file>]` → 期望态 YAML(active+disabled,过滤墓碑态,只 ref)到 stdout 或文件。

- [ ] **Step 1: 写失败集成测** — 在 `tests/integration/catalog-p3.test.ts` 加(复用同 ctx):

```ts
import { parseCatalogYaml } from '@/lib/catalog/serialize'
import { computeCatalogDiff, hasChanges } from '@/lib/catalog/diff'
import { toCatalogApps } from '@/lib/catalog/serialize'

describe('export round-trip', () => {
  it('getCurrent().yaml 再 apply(check)diff 空', async () => {
    await createCatalogService(ctx).apply({
      yaml: `version: 1\napplications:\n  - code: exp\n    name: Exp\n    keycloak: { clientId: exp-client, accessRole: exp_access }\n    roles: [{ code: admin, name: 管理员 }]\n`,
      source: 'cli',
    })
    const { yaml } = await createCatalogService(ctx).getCurrent()
    const doc = parseCatalogYaml(yaml)
    const curApps = await ctx.db.query.applications.findMany()
    const curRoles = await ctx.db.query.applicationRoles.findMany()
    const diff = computeCatalogDiff(toCatalogApps(curApps, curRoles), doc.applications)
    expect(hasChanges(diff)).toBe(false)
  })
})
```

- [ ] **Step 2: 跑测确认失败/通过判断** — `pnpm test:integration` → 此段应先 FAIL 若尚未有数据往返问题;若 getCurrent 已能往返则本段直接 PASS(它验证的是既有 getCurrent 的正确性,作为 export 内容的回归护栏)。无论如何,继续实现 CLI。

- [ ] **Step 3: 实现 export-catalog** — `scripts/export-catalog.ts`:

```ts
/**
 * 业务应用目录 export(声明式,DB → YAML):
 *   pnpm export-catalog                 # 打到 stdout
 *   pnpm export-catalog -- --out out.yaml
 * 内容 = 当前期望态(active+disabled;过滤 pending_deactivate/deactivated;只出 secretRef,不出明文)。
 * 前置:DATABASE_URL。
 */
import 'dotenv/config'
import { writeFile } from 'node:fs/promises'
import { createDbClient } from '@/lib/db/client'
import { parseCatalogYaml } from '@/lib/catalog/serialize'
import { scanForPlaintextSecrets } from '@/lib/catalog/secret-scan'
import { createKeycloakAdmin, keycloakConfigFromEnv } from '@/lib/keycloak/admin-client'
import { createCatalogService } from '@/server/services/catalog-service'
import type { ServiceContext } from '@/server/services/context'

function argValue(flag: string): string | undefined {
  const i = process.argv.indexOf(flag)
  return i >= 0 ? process.argv[i + 1] : undefined
}

async function main() {
  const url = process.env.DATABASE_URL
  if (!url) throw new Error('DATABASE_URL 未配置')
  const out = argValue('--out')
  const client = createDbClient(url)
  try {
    const ctx: ServiceContext = { db: client.db, keycloak: createKeycloakAdmin(keycloakConfigFromEnv()) }
    const { yaml } = await createCatalogService(ctx).getCurrent()
    const findings = scanForPlaintextSecrets(parseCatalogYaml(yaml))
    if (findings.length > 0) console.error(`[export-catalog] ⚠️ 疑似明文(仅路径): ${findings.map((f) => f.path).join(', ')}`)
    if (out) {
      await writeFile(out, yaml, 'utf8')
      console.error(`[export-catalog] 已写 ${out}`)
    } else {
      process.stdout.write(yaml)
    }
  } finally {
    await client.close()
  }
}

// 仅作为脚本直接运行时执行(被测试 import 时不跑)
if (process.argv[1] && process.argv[1].endsWith('export-catalog.ts')) {
  main().catch((error) => {
    console.error(error)
    process.exit(1)
  })
}
```

- [ ] **Step 4: package.json 脚本** — 在 `scripts` 里 `apply-catalog` 旁加:
  `"export-catalog": "tsx scripts/export-catalog.ts",`
  (与 `apply-catalog` 的 runner 保持一致——照抄 `apply-catalog` 那行的命令前缀,把脚本名换成 `export-catalog.ts`。)

- [ ] **Step 5: 跑测 + 冒烟** — `pnpm test:integration`(export round-trip 段 PASS);`pnpm typecheck` 无错;可选冒烟 `pnpm export-catalog | head`(需 DATABASE_URL)。

- [ ] **Step 6: Commit**

```bash
git add scripts/export-catalog.ts package.json tests/integration/catalog-p3.test.ts
git commit -m "feat(catalog): export-catalog CLI(DB → YAML,只出 ref)"
```

---

## Task 5: confirmDeactivate 服务 + 两个 API 端点

**Files:**
- Modify: `server/services/catalog-service.ts`(返回对象加 `confirmDeactivate`;import `and`/`ne`/`countActiveAssignments`)
- Create: `app/api/admin/catalog/pending-deactivate/route.ts`、`app/api/admin/catalog/deactivate/route.ts`
- Test: `tests/integration/catalog-p3.test.ts`(confirmDeactivate + 端点 + secret-scan 段)

**Interfaces:**
- Consumes: `countActiveAssignments`(Task 3);`ApiError`(`@/lib/http/api-error`);`adminRoute`/`ok`/`parseBody`(`@/app/api/_helpers`);audit(`this` 作用域内 `audit`、`actorOf`)。
- Produces:
  - `createCatalogService(ctx).confirmDeactivate(input: { appCode: string; roleCode?: string }): Promise<{ kind: 'app' | 'role'; appCode: string; roleCode?: string; status: 'deactivated'; affectedAssignments: number }>`
  - `GET /api/admin/catalog/pending-deactivate`(catalog:read)→ `{ items: PendingDeactivateItem[] }`
  - `POST /api/admin/catalog/deactivate`(catalog:apply)→ confirmDeactivate 结果

- [ ] **Step 1: 写失败集成测** — `tests/integration/catalog-p3.test.ts` 加(端点 import 放文件顶部 `vi.mock` 之后):

```ts
import { GET as pendingList } from '@/app/api/admin/catalog/pending-deactivate/route'
import { POST as deactivate } from '@/app/api/admin/catalog/deactivate/route'
import { eq } from 'drizzle-orm'
import * as schema from '@/db/schema'

describe('confirmDeactivate + 端点', () => {
  it('确认停用 pending_deactivate 应用 → deactivated + 从 getCurrent 消失', async () => {
    mockSession.current = adminSession
    await createCatalogService(ctx).apply({ yaml: `version: 1\napplications:\n  - code: dz\n    name: DZ\n    keycloak: { clientId: dz-client, accessRole: dz_access }\n    roles: []\n`, source: 'cli' })
    await createCatalogService(ctx).apply({ yaml: `version: 1\napplications: []\n`, source: 'cli' }) // dz → pending_deactivate

    const listRes = await pendingList(req('GET', '/api/admin/catalog/pending-deactivate'))
    expect(listRes.status).toBe(200)
    const listed = (await listRes.json()).data.items.map((i: { appCode: string }) => i.appCode)
    expect(listed).toContain('dz')

    const res = await deactivate(req('POST', '/api/admin/catalog/deactivate', { appCode: 'dz' }))
    expect(res.status).toBe(200)
    expect((await res.json()).data.status).toBe('deactivated')

    const row = await ctx.db.query.applications.findFirst({ where: eq(schema.applications.code, 'dz') })
    expect(row?.status).toBe('deactivated')
    const { yaml } = await createCatalogService(ctx).getCurrent()
    expect(yaml).not.toContain('code: dz')
  })

  it('对非 pending_deactivate 目标 → 409 CONFLICT', async () => {
    mockSession.current = adminSession
    await createCatalogService(ctx).apply({ yaml: `version: 1\napplications:\n  - code: live\n    name: Live\n    keycloak: { clientId: live-client, accessRole: live_access }\n    roles: []\n`, source: 'cli' })
    const res = await deactivate(req('POST', '/api/admin/catalog/deactivate', { appCode: 'live' }))
    expect(res.status).toBe(409)
  })

  it('apply 含明文密钥 → 400 VALIDATION_ERROR(details 只出路径)', async () => {
    mockSession.current = adminSession
    const res = await applyCatalog(req('POST', '/api/admin/catalog/apply', {
      yaml: `version: 1\napplications:\n  - code: leak\n    name: Leak\n    keycloak: { clientId: leak-client, accessRole: leak_access }\n    loginUrl: https://x/h?token=supersecretplaintext\n    roles: []\n`,
    }))
    expect(res.status).toBe(400)
    const body = await res.json()
    expect(JSON.stringify(body)).not.toContain('supersecretplaintext')
  })
})
```
  (`applyCatalog` 端点 import 若未在文件顶部,补 `import { POST as applyCatalog } from '@/app/api/admin/catalog/apply/route'`。)

- [ ] **Step 2: 跑测确认失败** — `pnpm test:integration` → FAIL(`confirmDeactivate`/端点不存在)。

- [ ] **Step 3: 实现 confirmDeactivate** — `server/services/catalog-service.ts`:
  - 顶部 import:`import { and, desc, eq, ne } from 'drizzle-orm'`(加 `and`、`ne`);`import { createCatalogReconcileService, countActiveAssignments, type ReconcileReport } from './catalog-reconcile-service'`(加 `countActiveAssignments`)。
  - 在返回对象里(`rollback` 旁)加:

```ts
    async confirmDeactivate(input: { appCode: string; roleCode?: string }): Promise<{
      kind: 'app' | 'role'; appCode: string; roleCode?: string; status: 'deactivated'; affectedAssignments: number
    }> {
      const flipped = await ctx.db.transaction(async (tx) => {
        const app = await tx.query.applications.findFirst({ where: eq(schema.applications.code, input.appCode) })
        if (!app) throw new ApiError('NOT_FOUND', `应用 ${input.appCode} 不存在`)
        if (input.roleCode) {
          const role = await tx.query.applicationRoles.findFirst({
            where: and(eq(schema.applicationRoles.applicationId, app.id), eq(schema.applicationRoles.code, input.roleCode)),
          })
          if (!role) throw new ApiError('NOT_FOUND', `角色 ${input.appCode}/${input.roleCode} 不存在`)
          if (role.status !== 'pending_deactivate') throw new ApiError('CONFLICT', `角色 ${input.roleCode} 非待停用态`)
          await tx.update(schema.applicationRoles).set({ status: 'deactivated', updatedAt: new Date() }).where(eq(schema.applicationRoles.id, role.id))
          return { kind: 'role' as const, appId: app.id, roleId: role.id }
        }
        if (app.status !== 'pending_deactivate') throw new ApiError('CONFLICT', `应用 ${input.appCode} 非待停用态`)
        await tx.update(schema.applications).set({ status: 'deactivated', updatedAt: new Date() }).where(eq(schema.applications.id, app.id))
        // 级联:该应用下所有非 deactivated 角色 → deactivated(应用已终态,角色随之)
        await tx.update(schema.applicationRoles).set({ status: 'deactivated', updatedAt: new Date() })
          .where(and(eq(schema.applicationRoles.applicationId, app.id), ne(schema.applicationRoles.status, 'deactivated')))
        return { kind: 'app' as const, appId: app.id }
      })

      const affected = flipped.kind === 'app'
        ? await countActiveAssignments(ctx.db, { appId: flipped.appId })
        : await countActiveAssignments(ctx.db, { roleId: flipped.roleId })

      await audit.append({
        ...actorOf(),
        action: 'catalog.deactivate',
        targetType: flipped.kind === 'app' ? 'application' : 'application_role',
        targetId: flipped.kind === 'app' ? flipped.appId : flipped.roleId,
        afterData: { appCode: input.appCode, roleCode: input.roleCode, status: 'deactivated', affectedAssignments: affected },
        result: 'success',
      })
      return { kind: flipped.kind, appCode: input.appCode, roleCode: input.roleCode, status: 'deactivated', affectedAssignments: affected }
    },
```

- [ ] **Step 4: 实现 GET 端点** — `app/api/admin/catalog/pending-deactivate/route.ts`:

```ts
import { adminRoute, ok } from '@/app/api/_helpers'
import { createCatalogReconcileService } from '@/server/services/catalog-reconcile-service'

export const GET = adminRoute({ permission: 'catalog:read' }, async (_request, { requestId, ctx }) => {
  const { pendingDeactivate } = await createCatalogReconcileService(ctx).detectDrift()
  return ok({ items: pendingDeactivate }, requestId)
})
```

- [ ] **Step 5: 实现 POST 端点** — `app/api/admin/catalog/deactivate/route.ts`:

```ts
import { z } from 'zod'
import { adminRoute, ok, parseBody } from '@/app/api/_helpers'
import { createCatalogService } from '@/server/services/catalog-service'

const deactivateSchema = z.object({ appCode: z.string().min(1), roleCode: z.string().min(1).optional() })

export const POST = adminRoute({ permission: 'catalog:apply' }, async (request, { requestId, ctx }) => {
  const body = await parseBody(request, deactivateSchema)
  const result = await createCatalogService(ctx).confirmDeactivate(body)
  return ok(result, requestId)
})
```

- [ ] **Step 6: 跑测确认通过** — `pnpm test:integration` → 本任务三段 PASS(其余仅容忍已知 flake);`pnpm typecheck` 无错。

- [ ] **Step 7: Commit**

```bash
git add server/services/catalog-service.ts app/api/admin/catalog/pending-deactivate/route.ts app/api/admin/catalog/deactivate/route.ts tests/integration/catalog-p3.test.ts
git commit -m "feat(catalog): confirmDeactivate 软停用 + pending-deactivate/deactivate 端点"
```

---

## Task 6: 控制台「待停用」面板 + hooks + i18n + e2e

**Files:**
- Modify: `features/catalog/queries.ts`(类型 + key + 两个 hook)
- Create: `features/catalog/pending-deactivate-panel.tsx`
- Modify: `features/catalog/catalog-view.tsx`(挂载面板)
- Modify: `messages/en/catalog.json`、`messages/zh-CN/catalog.json`
- Test: `tests/e2e/catalog-pending-deactivate.spec.ts`(新建)

**Interfaces:**
- Consumes: `apiFetch<T>(path, { method, json })`(`@/features/shared/api`);`ApiClientError`;`catalogKeys`;shadcn `Button`/`Card`/`AlertDialog`(见 `version-history.tsx` 用法);`useTranslations('catalog')`;sonner `toast`。
- Produces:`usePendingDeactivate()`、`useConfirmDeactivate()`、`<PendingDeactivatePanel/>`。

- [ ] **Step 1: queries hooks** — `features/catalog/queries.ts`:
  - `catalogKeys` 里加:`pendingDeactivate: () => [...catalogKeys.all, 'pending-deactivate'] as const,`
  - 文件末尾加:

```ts
export type PendingDeactivateItem = { kind: 'app' | 'role'; appCode: string; roleCode?: string; name: string; affectedAssignments: number }

export function usePendingDeactivate() {
  return useQuery({
    queryKey: catalogKeys.pendingDeactivate(),
    queryFn: () => apiFetch<{ items: PendingDeactivateItem[] }>('/api/admin/catalog/pending-deactivate'),
  })
}

export function useConfirmDeactivate() {
  const qc = useQueryClient()
  return useMutation({
    mutationFn: (input: { appCode: string; roleCode?: string }) =>
      apiFetch<{ affectedAssignments: number }>('/api/admin/catalog/deactivate', { method: 'POST', json: input }),
    onSuccess: () => qc.invalidateQueries({ queryKey: catalogKeys.all }),
  })
}
```

- [ ] **Step 2: i18n(先加文案,e2e/组件都依赖)** — `messages/en/catalog.json` 的 `catalog` 对象里,`versions` 旁加:

```json
    "pendingDeactivate": {
      "title": "Pending deactivation",
      "empty": "Nothing awaiting deactivation",
      "app": "App",
      "role": "Role",
      "affected": "{count} active assignment(s) affected",
      "confirm": "Confirm deactivate",
      "confirmTitle": "Confirm deactivation",
      "confirmBody": "Deactivate {name}? Keycloak roles are left untouched and {count} active assignment(s) are NOT revoked — it becomes a deactivated tombstone.",
      "done": "Deactivated {name}",
      "loadFailed": "Failed to load pending list"
    }
```
  `messages/zh-CN/catalog.json` 对应:

```json
    "pendingDeactivate": {
      "title": "待停用",
      "empty": "暂无待停用项",
      "app": "应用",
      "role": "角色",
      "affected": "影响 {count} 个生效分配",
      "confirm": "确认停用",
      "confirmTitle": "确认停用",
      "confirmBody": "停用 {name}?不会改动 Keycloak 角色,也不会撤销 {count} 个生效分配——它将变为 deactivated 墓碑态。",
      "done": "已停用 {name}",
      "loadFailed": "待停用列表加载失败"
    }
```

- [ ] **Step 3: i18n parity 测试通过** — 运行既有 parity 测试(通常 `pnpm test tests/unit/i18n-parity.test.ts` 或类似;若不确定文件名,`pnpm test -- i18n`)。Expected: PASS(en/zh-CN 键集一致)。

- [ ] **Step 4: 面板组件** — `features/catalog/pending-deactivate-panel.tsx`(AlertDialog 用法照抄 `version-history.tsx`;确认框复述影响数):

```tsx
// features/catalog/pending-deactivate-panel.tsx
'use client'
import { useState } from 'react'
import { useTranslations } from 'next-intl'
import { toast } from 'sonner'
import { Button } from '@/components/ui/button'
import { Card } from '@/components/ui/card'
import {
  AlertDialog, AlertDialogAction, AlertDialogCancel, AlertDialogContent, AlertDialogDescription,
  AlertDialogFooter, AlertDialogHeader, AlertDialogTitle,
} from '@/components/ui/alert-dialog'
import { ApiClientError } from '@/features/shared/api'
import { usePendingDeactivate, useConfirmDeactivate, type PendingDeactivateItem } from './queries'

export function PendingDeactivatePanel() {
  const t = useTranslations('catalog')
  const { data, isPending, isError } = usePendingDeactivate()
  const confirm = useConfirmDeactivate()
  const [target, setTarget] = useState<PendingDeactivateItem | null>(null)

  if (isPending) return <div className="h-24 animate-pulse rounded bg-muted" />
  if (isError) return <Card className="p-3 text-sm text-danger">{t('pendingDeactivate.loadFailed')}</Card>

  const items = data?.items ?? []
  const onConfirm = () => {
    if (!target) return
    const name = target.name
    confirm.mutate(
      { appCode: target.appCode, roleCode: target.roleCode },
      {
        onSuccess: () => { toast.success(t('pendingDeactivate.done', { name })); setTarget(null) },
        onError: (e) => toast.error(e instanceof ApiClientError ? e.message : String(e)),
      },
    )
  }

  return (
    <Card className="p-3">
      <p className="mb-2 text-sm font-medium">{t('pendingDeactivate.title')}</p>
      {items.length === 0 ? (
        <p className="text-sm text-muted-foreground">{t('pendingDeactivate.empty')}</p>
      ) : (
        <ul className="flex flex-col gap-2">
          {items.map((it) => (
            <li key={`${it.kind}:${it.appCode}:${it.roleCode ?? ''}`} className="flex items-center justify-between gap-2 text-sm">
              <span>
                <span className="text-muted-foreground">{t(`pendingDeactivate.${it.kind}`)}</span>{' '}
                <code>{it.appCode}{it.roleCode ? `/${it.roleCode}` : ''}</code> — {it.name}{' '}
                <span className="text-xs text-muted-foreground">{t('pendingDeactivate.affected', { count: it.affectedAssignments })}</span>
              </span>
              <Button variant="outline" size="sm" onClick={() => setTarget(it)}>
                {t('pendingDeactivate.confirm')}
              </Button>
            </li>
          ))}
        </ul>
      )}
      <AlertDialog open={target !== null} onOpenChange={(o) => !o && setTarget(null)}>
        <AlertDialogContent>
          <AlertDialogHeader>
            <AlertDialogTitle>{t('pendingDeactivate.confirmTitle')}</AlertDialogTitle>
            <AlertDialogDescription>
              {target && t('pendingDeactivate.confirmBody', { name: target.name, count: target.affectedAssignments })}
            </AlertDialogDescription>
          </AlertDialogHeader>
          <AlertDialogFooter>
            <AlertDialogCancel>{t('cancel')}</AlertDialogCancel>
            <AlertDialogAction onClick={onConfirm} disabled={confirm.isPending}>
              {t('pendingDeactivate.confirm')}
            </AlertDialogAction>
          </AlertDialogFooter>
        </AlertDialogContent>
      </AlertDialog>
    </Card>
  )
}
```
  (若 `@/components/ui/alert-dialog` 的具体导出名与 `version-history.tsx` 不同,以 `version-history.tsx` 实际 import 为准照抄。)

- [ ] **Step 5: 挂载到控制台** — `features/catalog/catalog-view.tsx`:
  - import 加:`import { PendingDeactivatePanel } from './pending-deactivate-panel'`
  - 在 `<VersionHistory .../>` 之后(闭合 `</div>` 之前)加一行:`<PendingDeactivatePanel />`

- [ ] **Step 6: 写 e2e** — `tests/e2e/catalog-pending-deactivate.spec.ts`(自造 fixture:经编辑器新增临时 app → apply,再移除 → apply 使其 pending_deactivate,面板确认停用)。参考 `tests/e2e/catalog-console.spec.ts` 的选择器/登录 setup。断言:面板标题出现;完整往返后临时 app 出现在待停用并可确认;确认后 toast。若造 fixture 过脆,退化为断言面板标题渲染 + 空态文案,并在 PR 说明里注明 confirmDeactivate 的确定性覆盖在集成测(Task 5)。

```ts
import { test, expect } from '@playwright/test'

test('待停用面板渲染', async ({ page }) => {
  await page.goto('/admin/catalog')
  await expect(page.getByText(/待停用|Pending deactivation/)).toBeVisible()
})
```

- [ ] **Step 7: 跑 e2e + 静态** — `pnpm exec playwright test tests/e2e/catalog-pending-deactivate.spec.ts` → PASS;`pnpm lint`(确认无 setState-in-effect)、`pnpm typecheck`、`pnpm build` → 全绿。

- [ ] **Step 8: Commit**

```bash
git add features/catalog/queries.ts features/catalog/pending-deactivate-panel.tsx features/catalog/catalog-view.tsx messages/en/catalog.json messages/zh-CN/catalog.json tests/e2e/catalog-pending-deactivate.spec.ts
git commit -m "feat(catalog): 控制台待停用面板 + 确认停用 hooks + i18n + e2e"
```

---

## Task 7: 验证 + docpact + PR

**Files:** 无代码改动(除非前序发现回归)。

- [ ] **Step 1: 全量测试** — `pnpm test`(unit 全绿)、`pnpm test:integration`(catalog-p3 + catalog-api 全绿,仅容忍 Global Constraints 列出的已知 flake,且这些 flake 隔离运行须过)、`pnpm exec playwright test tests/e2e/catalog-pending-deactivate.spec.ts tests/e2e/catalog-console.spec.ts`(绿)。
- [ ] **Step 2: 静态** — `pnpm typecheck` · `pnpm lint` · `pnpm build` → 全绿。
- [ ] **Step 3: docpact** — 从**工作区根**跑:`scripts/docpact lint --root identity-center --merge-base origin/main`。按 P1/P2 做法清理:config coverage + 关键架构文档 content-update + `scripts/docpact review mark --root identity-center --path <doc> --commit <代码 SHA>` 消 missing-review。允许 4 个既有结构性 uncovered 残留(definition-of-done.md、kingbasees-*.md、openapi.yaml)。
- [ ] **Step 4: 开合并 PR** — 到 identity-center main:

```bash
gh pr create --base main --head feat/catalog-p3 --title "feat(catalog): P3 周期对账 + 停用闭环 + export + 明文扫描" --body "<摘要 + 测试结果 + docpact 状态>"
```
  （合并动作等用户确认后再执行,依 P1/P2 惯例。）

---

## Self-Review

**Spec coverage(§ 对应):** §5.1→T1;§5.5 secret-scan→T2;§5.2 detectDrift + §5.3 job→T3;§5.4 export→T4;§5.6 confirmDeactivate + 端点→T5;§5.6 前端 + §7 i18n/e2e→T6;§7 验证 + docpact→T7。§8 安全约束进 Global Constraints + 各任务测试(secret-scan 不泄值断言 T2/T5;confirmDeactivate 不动 KC 由「不 import keycloak」+ 审阅保证)。§10 开放点:①面板归属=控制台(已定,T6);②export KC 依赖(照抄 apply-catalog,同 env 要求);③job ensure=active(loadActiveReconcileApps 只取 active,T3);④计数口径(app→applicationAssignments、role→applicationUserRoles,T3);⑤deactivated 不可逆(约束,无代码)。全覆盖。

**Placeholder scan:** 无 TBD/TODO;每个代码步含完整代码;测试步含真实断言;命令均具体。Step 引用的类型/函数均在本计划或 grounding 定义。

**Type consistency:** `PendingDeactivateItem`(T3 定义,T5 端点/T6 前端复用,字段一致 kind/appCode/roleCode/name/affectedAssignments);`countActiveAssignments(db, target)`(T3 定义,T5 消费);`ReconcileApp{code,keycloakClientId,accessClientRole}`(既有,T3 loadActive 复用);`confirmDeactivate` 返回型 T5 定义与 T6 mutation 使用一致;`ServiceContext={db,keycloak}` vs `JobContext={db,mq,keycloak}`(T3 job 用 `{db:ctx.db,keycloak:ctx.keycloak}` 构 ServiceContext,正确)。`ApiError(code,message,details?)`、`ok(data,requestId)`、`adminRoute({permission},handler)`、`apiFetch(path,{method,json})` 均与 grounding 一致。

**依赖顺序:** T2→T4(export 用 secret-scan);T1→T4/T5(getCurrent 过滤 deactivated);T3→T5(countActiveAssignments)→T6(hooks/类型);T3 detectDrift→T5 GET 端点。顺序 T1..T7 满足。
